<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.springboot.mapper.AAAAAdminMapper">

    <select id="selectAll" resultType="com.example.springboot.entity.AAAAAdmin">
        select * from `admin`
        <where>
            <if test="admin != null">admin like concat('%', #{admin}, '%')</if>
        </where>
        order by id desc
    </select>

    <insert id="insert">
        insert into `admin` (admin, password, phone, email)
        values(#{admin}, #{password}, #{phone}, #{email})
    </insert>

    <update id="updateById">
        update `admin` set admin = #{admin}, password = #{password}, phone = #{phone}, email = #{email}
        where id = #{id}
    </update>

</mapper>